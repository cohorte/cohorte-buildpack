#!/usr/bin/env bash
# bin/release <build-dir>

# must output yaml: specify addons, and Procfile entries (app entrypoint).

cat << EOF
addons: []
config_vars: []
default_process_types:
  web: bash $COHORTE_BASE/run --use-config $COHORTE_BASE/conf/run.js --web-admin \$PORT
EOF
